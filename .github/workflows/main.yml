name: GH200-Training

on: push

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Hello from GH200 Session"

      - name: Checkout
        uses: actions/checkout@v6

      - name: List files
        run: ls -la
